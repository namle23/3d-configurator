Ver1.
Requirements:
-Default component of the product

-Selecting/hovering separate object (blur the rest)

-Show the options (list all options) of the selected object (in 3D)
-Update the model based on the chosen options

-Show price
-Enable engineering (modify, select the point/area where user enable 
to open new table to edit kets/values)


DONE:
-Angle views (top, bottom, left, right, rotation)
-Swap the model
-Enable changing background image (nice to have 3D image)